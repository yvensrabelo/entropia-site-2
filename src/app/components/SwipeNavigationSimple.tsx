'use client'

import React from 'react'

interface SwipeNavigationSimpleProps {
  children: React.ReactNode
}

export default function SwipeNavigationSimple({ children }: SwipeNavigationSimpleProps) {
  return <>{children}</>
}